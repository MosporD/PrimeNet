"""
Application feature packages (Flask blueprints and supporting code).

Each subpackage is self-contained (e.g. ``modules.network_map``, ``modules.sync``).
Import blueprints from ``<pkg>.routes`` in ``app.py``.
"""
