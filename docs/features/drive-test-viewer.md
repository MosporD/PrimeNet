# Drive Test Viewer

Render drive-test logs on a map/charts.

| | |
|---|---|
| Route | `/drive-test-viewer` |
| Module | `modules/drive_test_viewer/` |
| Access | all |
| Version | V1.0 |

## Purpose

Plot DT samples (log files), not OSS PM.

## Approach

Keep DT files out of git (`uploads/` / data root). Do not join DT to PM unless the UI already does.

## Progress

Dated work log: [`drive-test-viewer.progress.md`](drive-test-viewer.progress.md). Do not duplicate long history here — update the progress file when this feature changes. Keep **Plans** as the module NEXT.


## Plans

None parked.
